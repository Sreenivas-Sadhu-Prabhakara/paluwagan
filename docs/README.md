# docs

Place a screenshot of the app here named **`screenshot.png`**. The project README
references it at `docs/screenshot.png`.

Suggested capture: the group dashboard with the rotation ring and a few periods
marked paid, at roughly 1200–1600 px wide. Please use made-up member names in the
screenshot rather than real people's names — the names in a group are personal
data.

No binary screenshot is committed by default; add your own.
